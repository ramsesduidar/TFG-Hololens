using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public interface IGenCadena
{
    // Start is called before the first frame update
    string getCadena(string entrada)
    {
        return entrada;
    }

    
}
